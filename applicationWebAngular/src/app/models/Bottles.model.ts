export class BottleModel {

  top: string;
  left: string;
  requestName: string;
  width: string;
  requestId: string;

  constructor(top: string, left: string, width: string) {
    this.top = top;
    this.left = left;
    this.width = width;
   }
}
